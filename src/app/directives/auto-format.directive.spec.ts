import { AutoFormatDirective } from './auto-format.directive';

describe('AutoFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFormatDirective();
    expect(directive).toBeTruthy();
  });
});
